import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF00C6FF);      // azul header
  static const primaryDark = Color(0xFF0092CC);  // azul más oscuro
  static const accentOrange = Color(0xFFFF6A3D); // botón "Reporta ahora"
  static const background = Color(0xFFF5FCFF);   // fondo claro
  static const softBlue = Color(0xFFB9EEFF);     // pill "Últimos reportes"
  static const cardBackground = Color(0xFFFFFFFF);

  static const textPrimary = Color(0xFF002333);
  static const textSecondary = Color(0xFF4B5563);
  static const navIcon = Color(0xFF6B7280);
}
